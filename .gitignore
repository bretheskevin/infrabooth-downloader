# macOS
.DS_Store

.idea/
node_modules/

# Build output
dist/

# Remote control mobile app build output (embedded via rust-embed at compile time)
src-tauri/remote-dist/

# Environment files with secrets
.env
.env.local
src-tauri/.env

# Rust build artifacts
target/
Cargo.lock

# Test scripts with credentials
test_api_v2_auth.sh
.superpowers/
.worktrees/
.claude/scheduled_tasks.lock
.claude/worktrees

# graphify knowledge graph (local, regenerated by git hooks)
graphify-out

# husky internal scripts (regenerated by husky install)
.husky/_/

# Security audit (internal — never publish exploit details to the public repo)
docs/security-audit.md
