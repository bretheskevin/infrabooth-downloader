/// Buffer time in seconds before token expiration to trigger refresh.
/// Tokens are considered expired if they expire within this window.
pub const TOKEN_REFRESH_BUFFER_SECS: u64 = 300; // 5 minutes
