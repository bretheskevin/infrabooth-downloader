# Feature flags
#
# Toggle in-progress features without removing their code. Only booleans are
# supported. Lines starting with '#' are comments; blank lines are ignored.
# Unknown keys and non-boolean values cause a startup error (see
# src/lib/featureFlags.ts).

# Rekordbox export (settings sidebar entry + section). Work-in-progress.
rekordbox = false
